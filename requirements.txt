# ============================================================
# OpenSet Face Recognition — 人脸识别系统
# ============================================================
# Python 3.10 推荐 | CUDA 12.1 | PyTorch 2.5.1
# ============================================================
#
# 安装方式：
#   pip install -r requirements.txt
#
# ⚠️ 版本说明：
#   核心功能（人脸检测/识别）：版本较灵活，用 >= 即可
#   AI 肖像生成（PhotoMaker + SDXL）：版本必须精确匹配，否则各种报错
#   numpy 必须 < 2.0，兼容性要求
# ============================================================

# ---------------------- 核心识别引擎 ----------------------
numpy<2.0                          # ⚠️ 必须 1.x，2.x 不兼容
opencv-python>=4.5.0               # 图像处理
Pillow>=8.0.0                      # 图像读写
torch>=1.12.0                      # 深度学习框架（建议 >=2.0）

# ---------------------- 人脸检测 & 识别 ----------------------
mtcnn>=0.1.1                       # MTCNN 人脸检测器
scikit-learn>=0.24.0               # 机器学习工具
scikit-image>=0.18.0               # 图像处理增强
scipy>=1.7.0                       # 科学计算
pandas>=1.1.0                      # 数据处理
matplotlib>=3.3.0                  # 图表绘制
onnxruntime>=1.17.0                # ONNX 推理运行时

# ---------------------- Web 框架 ----------------------
fastapi>=0.100.0                   # REST API 框架
uvicorn>=0.20.0                    # ASGI 服务器
python-multipart>=0.0.6            # 文件上传支持
requests>=2.28.0                   # HTTP 客户端

# ---------------------- 数据库 & 认证 ----------------------
sqlalchemy>=2.0.0                  # ORM
passlib[bcrypt]>=1.7.4             # 密码哈希
python-jose[cryptography]>=3.3.0   # JWT 编解码

# ---------------------- 开发工具（可选） ----------------------
pytest>=6.0.0                      # 测试框架
black>=21.0.0                      # 代码格式化

# ==================== AI 肖像生成（可选模块） ====================
# ⚠️ 以下依赖版本必须精确匹配，否则会出现各种兼容性错误！
# ⚠️ 如果不需要 AI 肖像功能，可以跳过以下所有依赖
#
# 额外安装步骤：
#   pip install git+https://github.com/TencentARC/PhotoMaker.git

diffusers==0.29.2                  # ⚠️ 精确版本！0.30+ 需要 huggingface_hub>=1.0
transformers==4.43.0               # ⚠️ 精确版本！5.x 与 diffusers 不兼容
huggingface_hub==0.36.2            # ⚠️ 精确版本！1.x 需要 transformers>=5.x
accelerate>=0.20.0                 # 模型加速
safetensors>=0.3.0                 # 安全序列化
peft>=0.6.0                        # LoRA / Adapter 支持
